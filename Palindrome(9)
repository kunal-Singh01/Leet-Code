class Solution {
    public boolean isPalindrome(int x) {
        String s=Integer.toString(x);
        String ans="";
        for(int i=s.length()-1;i>=0;i--){  //ulta for-loop
            ans+=s.charAt(i);
        }
        return (s.equals(ans)); //to compare values to two strings
    }
}
